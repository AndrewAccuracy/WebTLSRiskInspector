# 网络信息安全风险技术编程作业报告

## 1. 题目

本次平时作业题目为《本地网络资产与 Web 安全配置风险检测系统的设计与实现》。

本课题属于网络信息安全风险技术编程范畴，聚焦网络安全、系统运维安全与应用安全的交叉场景。项目通过自主编写 Python 程序，对主机端口暴露情况、Web 服务安全响应头、目录索引暴露、服务版本泄露以及 HTTPS 基础安全配置进行检测，从而把抽象的安全风险转化为可执行、可量化、可展示的结果。

## 2. 选题背景与研究意义

在实际网络环境中，许多安全事件并不是由高级 0day 漏洞直接造成，而是由于基础安全配置不到位引发。例如测试端口未关闭、目录索引暴露、HTTP 缺少安全头、旧版本服务直接对外暴露、HTTPS 证书过期等。这些风险往往门槛低、发现晚、利用快，一旦与弱口令、错误权限配置或供应链漏洞叠加，就容易进一步演化为数据泄露、横向移动甚至业务中断。

因此，本项目选择“本地网络资产与 Web 安全配置风险检测”作为编程对象，一方面因为它覆盖了网络安全与应用安全的基础风险面，另一方面也便于通过代码实现风险检测、风险评分和风险展示，体现“让风险可见、可测、可感”的课程要求。

## 3. 需求分析

根据课程要求，作业应当围绕网络信息安全风险技术开展编程实现，而不是简单调用现成第三方已编译安全工具。因此，系统设计必须体现以下几点：

1. 主要风险检测逻辑由本人编写程序完成；
2. 输出结果不能只停留在命令行过程，而应当具有可视化、结构化或报告化表达；
3. 应能够对风险进行基础解释、评级和整改建议输出；
4. 应给出执行结果与风险展示材料，证明系统能够运行。

结合以上要求，本系统的核心需求被定义为：

1. 能够扫描目标主机的指定端口；
2. 能够识别常见高风险端口或敏感服务暴露；
3. 能够对 HTTP 服务进行响应头与目录索引等基础安全检查；
4. 能够对 HTTPS 服务进行证书到期和 TLS 版本的基础检查；
5. 能够根据规则形成风险分值、风险等级和整改建议；
6. 能够生成 JSON 与 Markdown 报告，便于后续导出成 PDF。

## 4. 系统设计

### 4.1 总体架构

系统采用“端口探测 + 协议分析 + 规则评分 + 报告生成”的四层结构：

1. 端口探测层：负责探测指定端口是否开放；
2. 协议分析层：对开放的 HTTP/HTTPS 端口发起基础访问，提取响应头与证书信息；
3. 规则评分层：依据预定义规则判断风险项并计算分值；
4. 报告生成层：将检测结果汇总为 JSON 和 Markdown 格式，形成结构化输出。

### 4.2 模块划分

系统共划分为五个核心模块：

1. 参数解析模块  
负责读取目标主机、端口列表、超时时间和输出路径等运行参数。

2. 端口扫描模块  
通过 `socket.connect_ex()` 对目标端口进行连通性探测，识别开放端口。

3. HTTP 风险检测模块  
使用 `http.client` 访问目标 Web 服务，提取响应头并判断是否存在以下问题：
   - 缺少 HSTS；
   - 缺少 CSP；
   - 缺少 X-Frame-Options；
   - 缺少 X-Content-Type-Options；
   - 缺少 Referrer-Policy；
   - Server 精确版本暴露；
   - 目录索引暴露。

4. HTTPS 风险检测模块  
通过 `ssl` 建立 TLS 连接，提取证书有效期、协商协议版本及密码套件信息，识别证书异常和过期风险。

5. 报告生成模块  
将检测项按照标题、严重程度、证据、分值和整改建议进行归档，最后生成 JSON 与 Markdown 报告。

### 4.3 数据结构设计

为便于结果组织与后续扩展，系统设计了两个核心数据对象：

1. `PortResult`：用于存储端口号、开放状态、服务类型和风险提示；
2. `Finding`：用于存储风险标题、风险等级、风险分值、证据与建议。

这种结构具有较好的扩展性。后续若要加入弱口令检测、目录枚举、默认口令模拟、Nginx/Apache 专项配置分析等内容，只需继续扩展 `Finding` 生成规则即可。

### 4.4 风险评分机制

系统采用简单但清晰的规则评分模型。

1. 端口暴露风险评分  
例如 Telnet、Redis、MySQL 等敏感端口若对外开放，赋予更高基础分值；HTTP/8080 等普通 Web 端口则赋予较低基础分值。

2. Web 配置风险评分  
响应头缺失根据防护价值赋予不同分值，例如缺少 HSTS、CSP 会带来更高分值。

3. HTTPS 风险评分  
证书过期、TLS 版本过旧或密码套件过弱会被判定为高风险项。

4. 综合等级判定  
根据总分以及高风险项数量，将结果划分为高风险、中风险和低风险三个等级。

这一机制虽然不像工业级安全平台那样复杂，但足以完成课程作业对“风险量化”的要求。

## 5. 关键实现说明

### 5.1 端口扫描实现

端口扫描通过 Python 标准库 `socket` 完成。程序对给定端口逐个建立 TCP 连接测试，若返回值为 0，则说明目标端口开放。

该实现方式的优点包括：

1. 不依赖第三方扫描器；
2. 逻辑清晰、易于阅读和二次开发；
3. 能直接与后续风险规则联动。

### 5.2 HTTP 风险检测实现

系统使用 `http.client.HTTPConnection` 对开放的 Web 端口发起 GET 请求。获取响应头后，程序会进行一系列规则判断。例如：

1. 若 `Server` 响应头包含精确版本号，则判定为“版本信息暴露”；
2. 若缺少 HSTS、CSP 等头部，则分别生成对应风险项；
3. 若页面正文中出现 `Index of /`，则判定为目录索引暴露。

这种方式体现了“通过协议交互识别风险”的编程思想，而不是简单静态匹配文件内容。

### 5.3 HTTPS 风险检测实现

系统使用 `ssl.create_default_context()` 建立 TLS 连接，并在握手成功后获取：

1. 证书信息；
2. 协商出的协议版本；
3. 当前密码套件；
4. 证书过期时间。

随后程序依据规则进行分析，例如：

1. 证书是否已过期或即将过期；
2. TLS 是否仍为 1.0/1.1；
3. 密码套件是否含 RC4、3DES 等弱算法。

### 5.4 报告输出实现

程序会同时生成两种报告：

1. `scan_result.json`：便于后续进行结构化处理、统计和二次开发；
2. `scan_report.md`：适合直接阅读，也方便导出为 PDF 提交。

Markdown 报告中包含端口状态表、风险明细列表和风险统计摘要，能够较完整地体现作业结果。

## 6. 运行环境与实验设计

### 6.1 运行环境

本项目的实现环境如下：

1. 编程语言：Python 3
2. 主要依赖：Python 标准库（`socket`、`ssl`、`http.client`、`argparse`、`json` 等）
3. 操作系统：macOS / Linux / Windows 均可运行

本项目尽量只使用标准库，目的在于凸显“自主编程实现”的要求，避免把作业变成工具封装。

### 6.2 演示场景设计

为了证明系统能够真实发现风险，本项目额外设计了一个本地弱安全配置演示服务 `demo_vulnerable_server.py`。该服务具备如下特征：

1. 对外监听 8080 端口；
2. 响应头中显式返回 `Apache/2.4.49` 版本信息；
3. 不返回常见安全响应头；
4. 页面内容模拟目录索引暴露场景。

然后再使用主程序 `risk_scanner.py` 对本地地址 `127.0.0.1` 进行检测，从而获得可复现实验结果。

## 7. 执行过程

### 7.1 启动本地演示服务

在终端中运行：

```bash
python3 demo_vulnerable_server.py
```

系统会在本地启动一个 HTTP 服务，地址为 `http://127.0.0.1:8080`。

### 7.2 运行风险检测程序

在另一终端执行：

```bash
python3 risk_scanner.py --host 127.0.0.1 --ports 80,443,8080
```

程序会依次检测端口连通性，对开放的 Web 服务发起请求，并自动完成风险分析与报告生成。

## 8. 执行结果

本次在本地演示环境中的真实执行结果如下。

### 8.1 扫描命令

```bash
python3 risk_scanner.py --host 127.0.0.1 --ports 80,443,8080
```

### 8.2 端口扫描结果

| 端口 | 状态 | 服务类型 | 说明 |
| --- | --- | --- | --- |
| 80 | closed | HTTP | 当前未启用明文 Web 服务 |
| 443 | closed | HTTPS | 当前未启用 HTTPS 服务 |
| 8080 | open | HTTP-Alt | 本地演示弱安全配置服务处于开放状态 |

### 8.3 风险检测结果汇总

本次扫描共识别出 8 个风险项，其中高风险 1 项、中风险 5 项、低风险 2 项，综合风险分值为 76，系统给出的总体等级为“中风险”。

识别出的主要风险包括：

1. 目录索引暴露；
2. 8080 测试端口对外开放；
3. 缺少 Strict-Transport-Security；
4. 缺少 Content-Security-Policy；
5. Server 精确版本信息暴露；
6. 缺少 X-Frame-Options；
7. 缺少 X-Content-Type-Options；
8. 缺少 Referrer-Policy。

### 8.4 关键输出示例

程序执行完成后生成了 `results/scan_result.json` 和 `results/scan_report.md` 两份文件。根据实际运行结果，报告中的核心结论如下：

1. 目标主机为 `127.0.0.1`；
2. 被识别的开放端口为 `8080`；
3. 页面内容含有 `Index of /`，因此触发目录索引暴露告警；
4. `Server` 响应头返回 `Apache/2.4.49`，存在版本信息泄露；
5. 综合风险等级被评估为“中风险”。

## 9. 风险展示与分析

### 9.1 风险一：8080 测试端口暴露

8080 常用于测试环境、临时服务、后台面板或代理服务。若测试端口长期暴露在生产网络中，通常意味着：

1. 访问控制不严格；
2. 配置可能比主站更弱；
3. 更容易遗留调试接口、默认路径或默认口令。

因此，对 8080 暴露给予基础风险分是合理的。

### 9.2 风险二：Server 版本信息暴露

演示服务返回了精确版本 `Apache/2.4.49`。这会使攻击者更容易进行漏洞画像与定向攻击。如果该版本存在公开漏洞，攻击成本会显著降低。

### 9.3 风险三：安全响应头缺失

缺少 HSTS、CSP、X-Frame-Options 等头部会带来如下问题：

1. 浏览器无法强制 HTTPS；
2. 页面更容易遭受点击劫持；
3. 内容加载来源缺少约束，XSS 防护能力不足；
4. MIME 混淆带来的风险增大；
5. 来源策略过于宽松，可能导致敏感路径泄露。

这些问题虽然不一定立刻造成漏洞利用，但会显著降低系统整体防护能力。

### 9.4 风险四：目录索引暴露

目录索引暴露会让攻击者直接枚举静态资源、日志目录、配置目录或备份文件，一旦存在误放置的压缩包、配置文件、数据库转储文件，就可能导致严重的信息泄露。

### 9.5 风险可见、可测、可感的体现

本项目很好地满足了课程要求中的三个关键词：

1. 可见  
风险被明确列出，并附带证据、标题和整改建议。

2. 可测  
每个风险项都配置了具体分值，最终可形成综合风险评分。

3. 可感  
通过端口表格、详细报告与目录索引页面示例，使用者能够直观感受到风险严重性。

### 9.6 风险结果的教学意义

本次实验虽然基于本地模拟环境，但它高度还原了真实业务中最常见的一类问题，即“不是复杂漏洞，而是基础配置失守”。这一点具有很强的课程教学意义。

1. 从攻击者视角看，目录索引、版本暴露和弱配置端口往往是信息收集阶段最容易拿到的突破口；
2. 从防守者视角看，这类问题的整改成本并不高，但如果缺乏巡检机制，往往会长期存在；
3. 从工程实践视角看，将这类检查写成自动化脚本，比人工上线后逐项排查更稳定、更可复用。

因此，本项目不仅是一次课堂作业实现，也体现了将“安全检查流程自动化”的工程思路。

## 10. 预防措施

针对本项目识别出的风险，可以给出如下预防与整改措施：

1. 最小暴露面原则  
关闭不必要对外端口，对必须开放的端口配置访问控制列表、VPN 或零信任接入机制。

2. 隐藏服务版本信息  
在 Nginx、Apache、Tomcat 等服务中关闭详细版本头部，减少被动指纹暴露。

3. 补充关键安全响应头  
在网关、反向代理或应用代码层统一增加 HSTS、CSP、X-Frame-Options、X-Content-Type-Options、Referrer-Policy 等配置。

4. 关闭目录浏览  
禁止 Web 目录自动索引，清理误上传的日志、备份包和配置文件。

5. 加强 HTTPS 管理  
及时更新证书，启用 TLS 1.2/1.3，关闭弱密码套件，并建立自动续期监控机制。

6. 建立持续巡检机制  
将本项目的扫描逻辑扩展为定时巡检任务，对关键主机和站点进行持续检测。

## 11. 创新点与不足

### 11.1 创新点

1. 使用较少依赖完成从扫描、检测、评分到报告输出的完整链路；
2. 结合本地弱配置演示服务，形成了可复现、可验证的实验过程；
3. 将网络层与 Web 配置层风险统一纳入一个轻量级检测框架中。

### 11.2 不足

1. 当前仅支持基础端口与 HTTP/HTTPS 检查；
2. 未实现弱口令爆破、目录枚举、指纹识别细分、漏洞库关联等更深层能力；
3. 风险评分仍是规则驱动模型，尚未引入资产重要度、暴露范围和业务敏感性等上下文因素。

## 12. 后续改进方向

后续可以继续从以下方向扩展：

1. 增加多线程扫描能力，提升大规模端口检测效率；
2. 支持导出 HTML 报告，提升展示效果；
3. 引入简单资产台账功能，支持多主机批量对比；
4. 对接漏洞情报源，基于版本号给出更细粒度的漏洞提示；
5. 增加截图采集功能，把风险页面自动嵌入报告。

## 13. 关键代码说明

为了体现“代码开发”部分的工作量和设计思路，本节对几个核心函数进行说明。

### 13.1 `scan_port()`

该函数负责单端口连通性检测。它通过创建 TCP Socket 并调用 `connect_ex()` 判断端口是否开放，同时结合预置服务表给出基础风险提示。该函数是整个扫描链路的入口。

### 13.2 `check_http_service()`

该函数负责对 HTTP 服务做基础安全检查，主要动作包括：

1. 发起 GET 请求；
2. 读取 `Server` 响应头；
3. 检查 HSTS、CSP、X-Frame-Options 等安全头是否存在；
4. 对页面正文进行快速预览，判断是否存在 `Index of /`。

该函数体现了从“服务可访问”到“服务是否安全配置”的深入分析过程。

### 13.3 `check_https_service()`

该函数在目标启用 HTTPS 时运行。它通过 TLS 握手获取证书和密码套件信息，能够进一步判断：

1. 证书是否过期；
2. 协议版本是否过旧；
3. 加密套件是否存在弱算法。

这一设计使系统具备了从 HTTP 配置检查扩展到 HTTPS 配置检查的能力。

### 13.4 `calculate_port_risk()` 与 `summarize()`

这两个函数分别负责：

1. 将开放端口映射为风险项；
2. 汇总所有风险分值并给出总体风险等级。

它们共同构成了本项目的风险量化基础，使输出结果不只是“有无问题”，而是进一步转化为“问题有多严重”。

### 13.5 `generate_markdown()`

该函数负责把原始扫描结果转换为更适合阅读和提交的 Markdown 报告。它会自动生成：

1. 基本信息；
2. 端口表格；
3. 风险明细；
4. 风险统计摘要。

这一步增强了结果展示能力，使项目更符合课程中“执行结果”和“风险展示”的评分要求。

## 14. 总结

本次作业围绕“网络信息安全风险技术编程”这一课程主题，自主实现了一个轻量级安全风险检测系统。系统能够对本地网络资产暴露和 Web 基础安全配置进行检测，并输出风险分值、等级和整改建议。整个过程覆盖了系统设计、代码开发、执行结果、风险展示和预防措施，满足课程作业的核心要求。

从实验结果来看，即使是一个简化的本地演示环境，也能够暴露出典型且具有现实意义的安全风险，例如测试端口暴露、版本信息泄露、安全头缺失和目录索引开放等问题。这说明基础安全配置检测具有很强的教学价值和工程价值。

综上，本项目不仅完成了课程作业要求，也为后续构建更完整的网络安全检测平台提供了一个可扩展原型。
